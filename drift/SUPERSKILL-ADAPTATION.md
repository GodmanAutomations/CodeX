# Superskill Adaptation

Source catalog:

`/Users/stephengodman/Candice-Code/list_of_superskills.py`

The file is a 754-line superskill name catalog. It is useful as a pattern
source, not as an executable Python module.

## First Adapted Patterns

- `detecting-container-drift-at-runtime`
- `hunting-for-startup-folder-persistence`

## CodeX Translation

Containers drift when runtime state no longer matches the intended image.
Agent rooms drift when startup, handoff, memory, eval, and global instruction
surfaces no longer agree about the intended self.

The room-native adaptation is:

```bash
/Users/stephengodman/Candice-Code/bin/codex-self-drift
```

It checks that Coding Anchor remains CodeX's all-time posture across:

- global Codex instructions
- local CodeX self card
- identity lock
- startup docs
- README and handoff
- room brief helper
- standalone preflight
- identity eval
- SQLite startup recall
- Coding Anchor packet doctor

## Rule

Borrow patterns from the superskill catalog, then rebuild them in CodeX's own
language and verification loop. Do not pretend a catalog entry is an installed
capability until a real local command, eval, or workflow exists.
