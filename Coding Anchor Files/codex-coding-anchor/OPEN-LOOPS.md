# Open Loops

## Active

- Keep improving this packet toward a capable CodeX-native coding agent.
- Dogfood the native next-slice queue until `bin/coding-anchor-next` and
  `bin/coding-anchor-start`, `bin/coding-anchor-done`, and
  `bin/coding-anchor-loop --dry-run` become the normal handhold after slice
  closeout.
- Add project-specific mission files only when a real project is booted here.
- Import sharper agentic workflow patterns only as small, inspected,
  CodeX-native protocols; do not blindly install another framework over this
  packet.
- Keep agentic communication concise: action, result, verification, reviewer,
  next handhold.

## Watch

- Do not let the old Anchor/Gemini archive become live identity context.
- Do not let this packet become a lore dump; keep it executable.
- If a launcher changes future-session behavior, say that current sessions do
  not move automatically.
- Do not mistake autonomy for recklessness: keep verification, secret hygiene,
  and hard stop gates intact.
- Do not let post-work review become theater; fix valid findings or clearly
  explain why they are deferred.
- Do not let the next-slice loop become a daemon or autopilot that crosses hard
  gates without Stephen.
- Do not confuse concise updates with silent work; keep meaningful phase-change
  updates and verification visible.

## Parked

- A read-only clean-room analysis of the old Anchor archive can happen later if
  Stephen wants a deeper pattern extraction pass.
- Global automation or LaunchAgent supervision for this packet is intentionally
  not built yet.
