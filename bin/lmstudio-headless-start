#!/usr/bin/env zsh
set -euo pipefail

LMS="/Users/stephengodman/.lmstudio/bin/lms"

if [[ ! -x "$LMS" ]]; then
  echo "LM Studio CLI not found at $LMS" >&2
  exit 1
fi

"$LMS" daemon up
"$LMS" server start --port 1234 --bind 127.0.0.1
"$LMS" status
